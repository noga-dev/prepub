/// Ensure that if files in package were modified
/// then changelog was updated to reflect that
void validateChangelogSync({
  required List<String> files,
  required List<String> exclusions,
}) {
  if (exclusions.isEmpty) {}
}
